
import scala.runtime.RichInt
import scala.runtime.RichInt
import scala.math._;

object MainTest extends App {
  println("hello world")
  println(1.to(110))
  var x: String = "d"
  var x1: RichInt = 1
  //Console.err.println((math.pow(2, 2)).toInt)
  
  var t = if(true) {x=x+"ddd";x};
  println(t)
}
